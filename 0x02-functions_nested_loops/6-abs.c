#include "main.h"
/**
 * int _abs - computes the absolut value of an integer
 * @i: the integer to compute
 * Return: i for positive -i for negative
 */
int _abs(int i)
{
	if (i < 0 )
		return (-i);
	return (i);
}

